/**
 * Created by Ralph Varjabedian on 11/29/17.
 *
 * A simple helper for the properties to use so that they can be centralized and documented
 */

module.exports = Object.freeze({
  "isTest": "isTest",  // if an action is categories as a test (for reporting purposes)
  "skipScreenshot": "skipScreenshot" // if this action should not take screenshot for it's step
});